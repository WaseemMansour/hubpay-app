import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  transferScreenImage: {
    alignItems: 'center', marginTop: 40, marginBottom: 60
  },
  transferScreenContent: {
    color: '#fff', lineHeight: 25, textAlign: 'center'
  }
});

export { styles };
